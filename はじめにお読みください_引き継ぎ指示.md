# はじめにお読みください（Claude Code 引き継ぎ指示）

このディレクトリは、石原先生のセミナー会員サイト案件の作業フォルダです。
このプロジェクトの作業者はコウジ（Passione / G TECH JAPAN）です。石原先生は完成物を「運用する」だけで、開発作業は一切行いません。資料や文章の中で対象を取り違えないでください。

## まず最初にやってほしいこと

1. 同じディレクトリにある `CLAUDE.md` を読み、このプロジェクトの仕様・前提をすべて把握する。
2. `index.html`（仕様コンフィギュレーターの本体）と `README.md` の中身を確認する。
3. 把握した内容を一度こちらに要約して見せ、認識が合っているか確認する。

## このプロジェクトの現状

- `index.html` … 構成（案内・申込・決済・拡散防止・閲覧期限・維持費方針）の6トグルを切り替えると、構成モード・要件充足・実装方法・概算費用が連動して切り替わる、外部依存なしの単一HTMLツール。GitHub Pages でそのまま動く。
- `README.md` … GitHub Pages 公開手順。
- `CLAUDE.md` … 仕様の核（会員無料・受講で課金、2モード構成、Bunny Stream 第一候補、拡散防止に視聴者名の焼き込みは使わない、視聴回数は動画単位の総再生数、閲覧期限は動画単位 など）をまとめたプロジェクト記憶ファイル。

## GitHub の状況

- リポジトリ `takane3751/seminar-site` は作成済み。
- これまで commit / push は Claude Code 側で代行してきた（コウジは GitHub の手動アップロード操作は行わない方針）。
- このディレクトリの中身を、いつも通り commit / push して GitHub に反映してほしい。必要なら GitHub Pages 公開（main / root）まで案内・実行する。

## 次にやりたいこと（移行後の最初のタスク候補）

- このディレクトリを git 管理下にして、`index.html` / `README.md` / `CLAUDE.md` を `takane3751/seminar-site` に commit / push する。
- その後、GitHub Pages での公開状態を確認する。
- 余力があれば、仕様ツールにコウジが先生へ提示するための説明文・表紙を足して、提案資料として仕上げる。

## 作業スタイル

- やり取りは日本語。
- GUIベースのワークフローを好む（ターミナル操作は最小限に）。
- 変更したら、何をなぜ変えたかを簡潔に説明する。
